const User = ({name, email}) => {
    return (
        <div style={{marginBottom: "16px"}}>
            <div>name: {name}</div>
            <div>email: {email}</div>
        </div>
    )
}
 
export default User